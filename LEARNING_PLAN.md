# LabVIEW 通信程序设计 · 学习链路计划

> 📖 **参考书**：《LabVIEW通信程序设计》（人民邮电出版社，ISBN 978-7-115-60196-4）
> 🎓 **前置要求**：零基础即可开始（会用电脑鼠标即可，无需编程经验）
> 🧭 **学习方法**：每个阶段 = 看书对应章节 → 亲手搭VI → 完成小项目 → 打卡
> 🔗 **与你项目的结合**：学完串口后，可给你的 STM32 智能农业项目写上位机（呼应之前规划的远程报警功能）

---

## 一、学习地图（总览）

```
阶段0 环境与基础 ──→ 阶段1 程序结构 ──→ 阶段2 数据/字符串
                                        │
                阶段3 串口通信 ⭐（重点，对接嵌入式）
                                        │
       阶段4 网络通信(TCP/UDP)  阶段5 仪器控制(VISA)
                                        │
                       阶段6 综合实战项目
```

**核心心法**：LabVIEW 不是写代码，是"连线"。前面板是仪表盘，程序框图是线路图。**数据流编程**——数据流到哪，程序就执行到哪，这就是LabVIEW的灵魂。

---

## 二、详细阶段

### 阶段0 · 环境与"第一次连线"（预计1天）
🎯 **目标**：装好LabVIEW，跑通第一个程序，认识两个界面。
- **安装**：NI官网下载 **LabVIEW Community版（社区版，免费）**，安装包较大（约4-5GB），装到 **E盘**（记住你的C盘原则！）
- **认识两个窗口**：
  - **前面板（Front Panel）**：用户看到的界面，放"控件"（旋钮/按钮/图表）和"指示器"（显示屏/灯）
  - **程序框图（Block Diagram）**：幕后连线的地方，放"函数"和"接线"
- **理解数据流**：C语言是一行行执行；LabVIEW是**数据一到位就执行**。连线 = 数据传递
- 🔧 **动手**：前面板放一个数值输入控件 → 框图连线到数值显示控件 → 点 ▶运行，转动旋钮看数值变化
- ✅ **完成标志**：能独立从零搭出一个"输入→显示"的VI，理解两个面板的关系

### 阶段1 · 程序结构（预计2天）
🎯 **目标**：掌握循环和条件，相当于C语言的 if/for/while。
- **While循环**：框住代码，满足条件才停（注意：必须设置停止条件，否则永远运行）
- **For循环**：固定次数循环，有"循环计数N"
- **条件结构（Case Structure）**：相当于 if-else，真/假两个分支
- **移位寄存器（Shift Register）**：循环之间传数据（上一圈的结果给下一圈用）——这是LabVIEW的精髓
- **事件结构（Event Structure）**：响应按钮点击等用户操作
- 🔧 **动手**：做一个"计数器"——按一次按钮数字+1；做一个"温度读取循环"（配合后面串口）
- ✅ **完成标志**：能独立写出 循环+条件 组合的程序

### 阶段2 · 数据与字符串处理（预计2天）⭐通信的前提
🎯 **目标**：掌握字符串和字节处理——**通信传的就是字节，这是核心前置**。
- **数值类型**：整型I8/I16/I32、浮点、布尔
- **字符串（String）**：字符数组；通信中重点是**十六进制字符串**和字节数组
- **格式化与转换**：数值↔字符串、十进制↔十六进制（相当于C的 printf/scanf）
- **数组与簇（Cluster）**：批量数据和打包数据
- 🔧 **动手**：写一个"进制转换器"；把一组传感器数值打包成字节序列
- ✅ **完成标志**：能手写"拼接协议帧、解析收到的字节流"

### 阶段3 · 串口通信 ⭐⭐（预计3天，重中之重）
🎯 **目标**：掌握串口收发——**你STM32项目对接电脑的入口**。
- **串口基础**：RS-232/485，波特率、数据位、停止位、校验位（和STM32的USART对应！）
- **VISA串口节点**（函数面板 → Instrument I/O → Serial）：
  - VISA配置串口（打开，设波特率9600等）
  - VISA写入 / VISA读取（读的是字节）
  - VISA关闭
- **串口通信三要素**：**打开→读写→关闭**（和文件操作一样）
- **接收难点**：串口数据随时来 → 用 While循环+超时 持续读，或事件驱动
- 🔧 **动手（关键项目）**：做一个**串口助手**——文本框发数据、图表收数据。然后用**虚拟串口工具**（如VSPD虚拟串口对）在本机测试，不需要真硬件！
- 🔗 **对接你的项目**：用你智能农业的 `AA55 cmd len data checksum`协议帧，写上位机解析温度/湿度/光照/土壤数据
- ✅ **完成标志**：串口助手能收发，能正确解析你STM32的自定义协议帧

### 阶段4 · 网络通信 TCP/UDP（预计2天）
🎯 **目标**：掌握电脑间网络通信。
- **TCP**：可靠连接。三个节点：**TCP监听（服务器端）** / **TCP打开（客户端）** / **TCP读写**
- **TCP同步问题**：发多少收多少的"对齐"——用消息定长/帧头识别
- **UDP**：无连接、快但不保证到达。UDP打开/UDP读写
- **远程前面板**：把VI发布成网页，浏览器远程控制
- 🔧 **动手**：本机开一个TCP服务器+客户端，互发消息；UDP做简单数据广播
- ✅ **完成标志**：能写一个TCP聊天小程序

### 阶段5 · 仪器控制 VISA（预计1天，了解）
🎯 **目标**：了解LabVIEW的老本行——控制仪器（万用表、示波器、程控电源）。
- **VISA是标准接口**：GPIB / USB / 串口 / 以太网仪器统一用VISA控制
- 与阶段3串口呼应：串口只是VISA的一种
- 若你有实验室仪器，可练习自动读取测量值
- ✅ **完成标志**：理解"仪器即节点"，能用VISA打开/读写一台设备

### 阶段6 · 综合实战（预计3天）
🎯 **目标**：把所有技能串起来，做一个完整项目。
- **项目建议（推荐）**：**智能农业上位机** ——
  - 串口接STM32，解析AA55协议帧
  - 前面板图表实时显示温/湿/光照/土壤
  - **超阈值报警**（呼应你规划的远程报警：温度>30、湿度>75、光照<50、土壤<30）
  - 数据记录到文件（CSV）
- **进阶架构**：生产者-消费者模式（串口持续收 + 界面实时刷新，不卡顿）
- ✅ **完成标志**：能独立完成这个上位机项目并演示

---

## 三、学习节奏建议（约3周）

| 周 | 内容 | 产出 |
|---|---|---|
| 第1周 | 阶段0+1+2 | 完成计数器、进制转换器 |
| 第2周 | 阶段3 串口（重点） | **串口助手 + 协议帧解析** |
| 第3周 | 阶段4+6 | TCP聊天 + 智能农业上位机 |

> 💡 串口（阶段3）是性价比最高的一环，学完它你就能立刻对接STM32项目，建议优先吃透。

---

## 四、配套资源
详见 [RESOURCES.md](RESOURCES.md)。

## 五、学习打卡清单
详见 [CHECKLIST.md](CHECKLIST.md)。
